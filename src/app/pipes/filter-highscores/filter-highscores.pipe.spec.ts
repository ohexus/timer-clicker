import { FilterHighscoresPipe } from './filter-highscores.pipe';

describe('FilterHighscoresPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHighscoresPipe();
    expect(pipe).toBeTruthy();
  });
});
